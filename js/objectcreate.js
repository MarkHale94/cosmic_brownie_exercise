const objectMaker = function (title, description, amount, topping, flavor, weight){
    const snackCake = {
    title: title,
    description: description,
    amount: amount,
    topping: topping,
    flavor: flavor,
    weight:weight
    }
    return snackCake
};



